<h5 class="title">3 Comments</h5>
<% if(typeof comments != "undefined"){ %>
<% comments.forEach(comment =>{ %>

<ol>
    <!-- Single Comment Area -->
    <li class="single_comment_area">
        <!-- Comment Content -->
        <div class="comment-content d-flex">
            <!-- Comment Author -->
            <div class="comment-author">
                <img src="/img/bg-img/<%= comment.images_src %>" alt="author">
            </div>
            <!-- Comment Meta -->
            <div class="comment-meta">
                <a href="#" class="post-author"><%= comment.author_name %></a>
                <a href="#" class="post-date"><%= dateAndTime.format(comment.comments_date, 'MMMM DD  | h:mm A') %></a>
                <p><%= comment.comments_text %></p>
            </div>
            <h1></h1>
        </div>
    </li>
</ol>
<% }) %>
<% } %>